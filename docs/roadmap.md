# C-Two Current Roadmap

Last reviewed and ordered: 2026-05-18.

This is the maintained roadmap for C-Two's current 0.x line. Historical design notes under `docs/plans/`, `docs/spikes/`, and `docs/superpowers/` are frozen archives; they may use older terminology such as ICRM, Component, `@cc.runtime.connect`, or old package paths.

## Status Definitions

| Status | Meaning |
| --- | --- |
| Stable | Implemented in the current architecture and expected to remain part of the public direction. |
| Planned | Still needed for the current C-Two direction, but not yet implemented as a complete public capability. |
| Future | Directionally important, but dependent on further design, downstream demand, or prerequisite capabilities. |
| Archive | Historical context only; do not treat as the current implementation plan. |

## Implementation Sequence

Roadmap work should be picked up in this order unless a later review explicitly supersedes it. The ordering is part of the roadmap: it minimizes protocol churn by stabilizing resource-to-CRM projection, contract identity, codec identity, and call envelopes before adding new call shapes, language SDKs, or governance surfaces.

| Order | Workstream | Why it comes here | Exit criteria |
| --- | --- | --- | --- |
| 1 | FastDB-first CRM ABI, bridge, and codegen foundation | Cross-language clients cannot depend on Python annotations, pickle fallback, or ad hoc transferable hooks. Existing resource classes still need a low-intrusion path into CRM projection, but the portable payload ABI for this workstream is fastdb call-db, not an open-ended payload subsystem. | `c-two.contract.v1` remains the Rust-validated callable contract; method input/output wire refs for portable methods resolve to fastdb call-db `PayloadAbiRef` values; Python-native fallback is explicitly Python-only and emits diagnostics for schema/codegen workflows; `cc.register(...)` supports a per-method resource bridge that applies to thread-local, direct IPC, and relay; fastdb exposes scalar, single feature, `Array[T]`, `Batch[T]`, tuple return, and object-graph planning; grid proves normal and held fastdb calls end to end with no `python-pickle-default` in the portable descriptor. |
| 2 | Contract version compatibility and descriptor stability | Semver and range matching need the language-neutral descriptor from order 1; otherwise compatibility rules would hard-code Python descriptor details. | Exact contract validation remains the safety floor; semver or range matching is Rust-owned; ambiguous or ABI-incompatible matches fail with structured errors. |
| 3 | `auth_hook` and call metadata for unary calls | Metadata changes every call envelope, so it must land before async, streaming, or SDK expansion multiply the number of call paths. | Thread-local, direct IPC, and HTTP relay paths can pass metadata; hooks can accept or reject calls; policy remains outside C-Two. |
| 4 | Dry-run hooks for unary calls | Dry-run depends on the hook/metadata surface and should be defined before more call shapes need the same semantics. | Dry-run does not execute resource side effects; unsupported call shapes fail explicitly; hook-visible metadata is documented. |
| 5 | Async unary API | Async should extend a stable unary envelope instead of becoming a second protocol design. | `cc.connect_async()` and async proxy/context-manager behavior exist for thread-local, direct IPC, and HTTP relay without regressing the sync API. |
| 6 | Rust-owned telemetry, backpressure, and adaptive memory lifecycle policy | Streaming will amplify memory retention and cancellation risks, so runtime policy needs to exist first. | Buddy decay, dedicated segment timeout, and chunk assembler timeout are separately modeled; telemetry is testable; Python remains a thin facade. |
| 7 | Streaming RPC and pipeline semantics | Streaming depends on async, metadata, cancellation, and backpressure. Existing chunked payload transfer is only a transport primitive. | Stream identity, lifecycle frames, cancellation, ordering, error propagation, and resource release are specified and covered by end-to-end tests. |
| 8 | Rust SDK first real slice | Rust can reuse the existing core crates directly and is the lowest-friction proof that the cross-language contract is usable. | A Rust client can call a Python-hosted CRM through a supported transport; contract mismatch fails; docs include runnable commands. |
| 9 | TypeScript client with fastdb codec | Browser-facing SDK work depends on stable descriptors, codec annotations, metadata, and compatibility semantics. | Node/browser support boundaries are explicit; only eligible codecs are exposed; relay/control-plane rules are not reimplemented differently in TypeScript. |
| 10 | Global discovery and namespace governance | Relay mesh already propagates routes; broader discovery is a governance/admin layer and must not weaken production contract-scoped resolution. | Discovery returns candidate route metadata through an explicit admin surface; normal calls still reject name-only or contract-mismatched resolution. |

## Handoff Rules

Future agents and human developers should start from the first incomplete workstream in the sequence above unless the user explicitly redirects the scope. Before implementation, read this roadmap, the relevant current code, [`docs/vision/cross-language-contract-codec-architecture.md`](./vision/cross-language-contract-codec-architecture.md), and the active fastdb-first plan [`docs/plans/2026-05-18-fastdb-first-crm-abi-and-bridge.md`](./plans/2026-05-18-fastdb-first-crm-abi-and-bridge.md); treat older `docs/plans/`, `docs/spikes/`, and `docs/superpowers/` files as historical evidence unless a current roadmap entry links to them explicitly.

Keep language-neutral runtime mechanisms in Rust core or FFI. Python and future SDKs should stay thin: typed facades, transferable serialization, and language ergonomics only. Do not put config resolution, relay routing, retries, caching, contract authority, scheduler policy, backpressure, or memory lifecycle policy in a single SDK.

Do not create speculative SDK scaffolding. A new SDK directory should appear only with a runnable slice, tests or examples, and documentation that proves the slice works end to end.

Each workstream should be split into small reviewable slices. For call-path changes, verify thread-local, direct IPC, and HTTP relay behavior when applicable; for protocol-affecting changes, include Rust core tests and Python integration coverage before calling the slice complete.

## Stable

| Capability | Current state |
| --- | --- |
| Core RPC framework | CRM contracts, resource registration, and client proxies are the active public model. |
| IPC transport with shared memory | Direct IPC and Rust-owned SHM allocation are core transport capabilities. |
| HTTP relay | `c3 relay` is the standalone Rust relay runtime; the Python SDK does not embed relay server lifecycle. |
| Relay mesh discovery | Relay peers propagate contract-scoped route announcements and withdrawals through the mesh. |
| Chunked payload transfer | Large payloads can cross IPC/relay boundaries through chunked transport. This is payload chunking, not user-facing Streaming RPC. |
| Heartbeat and connection management | IPC and relay runtimes own connection health and lifecycle management. |
| Read/write concurrency control | CRM method metadata feeds native route concurrency enforcement. |
| Unified configuration | Rust `c2-config` is the source of truth for environment, `.env`, and default resolution. |
| Multi-platform Python packaging | CI builds and publishes Python wheels for supported platforms and Python versions. |
| Disk spill for extreme payloads | Rust memory management can spill oversized payloads to files when configured thresholds require it. |
| Hold mode and `from_buffer` zero-copy | `cc.hold()` and transferable `from_buffer` support retained buffer views through native lease accounting. |
| SHM residence monitoring | `cc.hold_stats()` exposes SDK-visible retained buffer statistics. |

## Planned

| Capability | What remains | Implementation direction |
| --- | --- | --- |
| FastDB-first CRM ABI, bridge, and codegen foundation | C-Two now owns `c_two.fastdb` integration helpers, FastDB call-db planning, bridge derivation, FastDB-backed TypeScript helper generation through `c3 contract codegen typescript --fastdb-schema`, and cross-transport FastDB ABI smokes. FastDB owns authored value types, schema/storage/serialization/view/runtime substrate, and the `fastdb4py` annotation surface. | Keep portable CRM payloads on FastDB call-db or no-payload; keep pickle as Python-only fallback; keep C-Two runtime route/relay/IPC/scheduler/lease layers from parsing FastDB storage internals. |
| TypeScript IPC SHM response reader seam | Generated `createIpcEncodedTransport(...)` now decodes Rust buddy SHM reply metadata, preserves server handshake SHM prefix/segment metadata, calls injected `responseShmReader.read(block, destination?)` and `responseShmReader.release(block)` hooks, can pass allocator-owned destination views to that reader so FastDB-owned payload ingress also covers SHM replies without returning payloads that alias released mappings, includes a dedicated-only Node/POSIX helper that reads dedicated response segments and marks `read_done`, exposes structural native-buddy backend wrappers, has a Rust `c2-mem-ffi` response-pool substrate that can open deterministic server buddy segments, copy into caller-owned destinations, and release read non-dedicated blocks through c2-mem `free_at`, and now has both a buildable `@c-two/c2-mem-ffi` TypeScript binding-package adapter surface and generated c2-mem-ffi facade helpers that turn lower-level response pool factories into `responseShmReader` hooks. Generated TypeScript also now has a structural `createNodeIpcConnect({ net })` connector with a real `node:net` Unix-domain-socket frame smoke, and the FastDB grid TypeScript smoke composes that generated IPC path with the c2-mem-ffi Node loader against a real Python/Rust direct IPC server so Rust buddy replies are read, copied into FastDB-owned or generated owned-byte destinations, and released through c2-mem-ffi. Missing readers, missing release hooks, malformed buddy metadata, oversized SHM data sizes, invalid allocator outputs, and non-dedicated use of the dedicated helper fail at the transport boundary. | C-Two generated IPC helpers remain runtime-focused: they provide a narrow dedicated POSIX-file helper plus native-buddy wrapper contracts, while real c2-mem allocator mapping/free lives in Rust FFI/runtime code rather than generated TypeScript. Remaining work is broader real IPC coverage beyond the initial grid proof, publishable runtime packaging, and Bun/Deno or equivalent native/FFI loaders beyond the initial Node/POSIX C ABI loader. |
| TypeScript IPC chunked request transport | Generated `createIpcEncodedTransport(...)` now exposes `requestChunkSize`, keeps small requests inline, sends larger requests with Rust-compatible chunked call frames, requires server `CAP_CHUNKED` before chunked sends, rejects invalid chunk sizing and unrepresentable chunk counts before writing, and closes the connection after partial chunked request write failure. | This remains the request-side large-bytes fallback for FastDB call-db payloads and other codec bytes when no request SHM writer is configured or runtime constraints make SHM unavailable. |
| TypeScript IPC request SHM writer transport | Generated `createIpcEncodedTransport(...)` now accepts `requestShmWriter` and `requestShmThreshold`, advertises the writer's client SHM prefix and buddy segments in the IPC handshake, sends larger requests as Rust-compatible `FLAG_CALL_V2 | FLAG_BUDDY` frames with `[buddy_payload][call_control]`, validates writer block metadata against u16/u32 limits and advertised segment ranges, releases invalid or failed-write blocks locally, leaves non-dedicated successful blocks to the Rust server's cross-process free authority, releases dedicated blocks after a matching server response proves consumption, pairs that client contract with Rust server cleanup for unconsumed buddy blocks on early route-admission or capacity rejects, includes a dedicated-only Node/POSIX writer helper that creates page-aligned dedicated segments and unlinks them on release, exposes structural native-buddy backend wrappers for non-dedicated request writers, has a Rust `c2-mem-ffi` C ABI request-pool substrate, and now has both a buildable `@c-two/c2-mem-ffi` TypeScript binding-package adapter surface and generated c2-mem-ffi facade helpers that turn lower-level request pools into `requestShmWriter` hooks. Generated TypeScript also now has a structural `createNodeIpcConnect({ net })` connector with a real `node:net` Unix-domain-socket frame smoke, and the FastDB grid TypeScript smoke composes the generated IPC path with the c2-mem-ffi Node loader against a real Python/Rust server so request buddy frames are accepted, consumed by Rust authority, and marked consumed locally without client-side release. | C-Two generated IPC helpers remain runtime-focused: the generated helper covers dedicated POSIX-file segments only, while native-buddy wrappers delegate real allocator work to injected backends. Remaining work is broader real IPC coverage beyond the initial grid proof, publishable runtime packaging, and Bun/Deno or equivalent native/FFI loaders beyond the initial Node/POSIX C ABI loader over the request-side and response-side `c2-mem-ffi` substrates. |
| FastDB bridge coverage status note | Recent bridge guardrail slices have expanded the explicitly covered shapes beyond the broad line above: scalar bridge input/output coercion, WSTR/BYTES feature-output bridge and held-view access, downstream fastdb4ts WSTR/single-BYTES retained call-db column runtime and object-graph row runtime, generated-helper WSTR/single-BYTES object-graph smoke coverage, abstract container annotations, tuple-row and variadic tuple row containers, split-column containers, table-object factories, column-container inputs/outputs, and internal non-FastDB diagnostics now have source-to-sink tests. | Treat remaining bridge work as arbitrary synthesis outside these explicit shapes, not as proof gaps for the shapes already covered by the active plan phases. |
| FastDB call-db strict codec shape guardrail | C-Two strict TypeScript codegen now rejects `org.fastdb.call-db` wire refs that omit `schema="fastdb.call-db.schema.v1"`, use another schema, omit `schema_sha256`, or omit the `bytes` capability, so strict clients cannot be generated with payload requirements that FastDB cannot satisfy. | Keep C-Two validation limited to strict-mode admission rules for the selected portable CRM ABI, and keep deeper fastdb schema/table validation in FastDB-owned codegen/runtime. |
| TypeScript held `buffer-view` capability guardrail | Generated C-Two clients and generated codec stubs use `fromBuffer` only when the output codec requirement declares `buffer-view`; FastDB generated registries mirror that rule, so columnar held outputs can return `FastdbC2CallDbView` while object-graph/decode-only outputs materialize through `decode`. | Treat `buffer-view` as a contract capability, not an implementation accident. Generated binding registries may expose hooks only when descriptors advertise them, and generated C-Two hold paths must check the descriptor capability before trusting a hook. |
| TypeScript relay timeout parity | Generated TypeScript HTTP relay transports now bound explicit relay calls with `callTimeoutMs` and relay-aware route resolution with `resolveTimeoutMs`; both options reject fractional, negative, non-finite, or oversized values so `0` remains the only timeout-disable value. | Keep timeout, route cache, retry, and contract routing behavior in C-Two generated transport instead of FastDB helpers; FastDB codec code should only compose payload codecs over the C-Two transport. |
| TypeScript route cache TTL guardrail | Generated TypeScript relay-aware transport now applies the same non-negative safe-integer millisecond guardrail to `routeCacheTtlMs`, so fractional values cannot be truncated to `0` and silently disable route caching. | Keep cache semantics explicit: `0` disables cache, positive safe integers cache by full route contract identity, and invalid values fail at transport construction. |
| TypeScript route cache invalidation scope | Generated TypeScript relay-aware transport now invalidates stale route-cache entries by the same full route-contract key used for cache lookup, so one CRM contract's stale route cannot evict another contract sharing the same route name. | Keep route cache storage, refresh, stale invalidation, and current-route preference scoped by full CRM contract identity rather than route name alone. |
| TypeScript resolve transport retry parity | Generated TypeScript relay-aware resolve now classifies fetch failures, resolve body failures, invalid resolve JSON, and resolve timeout as `C2HttpTransportError`, and retries those errors up to `maxAttempts` like Rust relay-aware resolve. | Retry only the control-plane resolve transport/5xx class and stale-route data-plane class; do not replay ordinary CRM method failures or ambiguous data-plane failures. |
| TypeScript maxAttempts guardrail | Generated TypeScript relay-aware transport now matches Rust route-attempt semantics: `0` normalizes to one attempt, safe integers `1..=32` are accepted, and negative, fractional, non-finite, or oversized values fail at construction. | Keep retry policy deterministic and construction-time validated; do not truncate or clamp user input into a different retry policy. |
| TypeScript route contract identity guardrail | Generated TypeScript HTTP relay transports now validate the route contract schema, non-empty CRM namespace/name/version, and ABI/signature hashes before constructing expected CRM headers or resolve queries. | Public bytes transports should fail malformed caller-supplied contract identity locally; generated clients still use generated constants, but helper APIs must not let bad identity reach relay/network code. |
| TypeScript route contract text guardrail | Generated TypeScript HTTP relay transports now validate caller-supplied route contract namespace/name/version text against the Rust `c2-contract` CRM tag boundary, including 255-byte UTF-8 wire length, surrounding whitespace, control characters, and path/tag separators. | Keep public bytes transport contract identity validation aligned with Rust before expected-contract headers or relay resolve queries are built. |
| TypeScript route path text guardrail | Generated TypeScript HTTP relay transports now validate `routeName` and method strings before resolve or data-plane fetch, including 255-byte UTF-8 wire length, surrounding whitespace, control characters, and separator policy while preserving `/` as a valid route-name character. | Treat route and method path components as local SDK boundary inputs; malformed text must not become relay URLs, and generated TypeScript must stay aligned with Rust route/method validation. |
| TypeScript HTTP transport boundary guardrail | Generated TypeScript HTTP relay transports now validate transport options, absolute HTTP(S) base/anchor/resolved relay URLs without whitespace, credentials, query, or fragment components, `fetch`, and user headers at construction or resolve-payload normalization; custom headers may pass through, but `Content-Type` and C-Two expected-contract headers cannot be overridden, including by case variants. | Keep route contract and relay URL authority in generated C-Two transport. FastDB codec helpers should compose over this transport instead of accepting or rewriting reserved HTTP identity headers or malformed relay endpoints. |
| TypeScript HTTP response length guardrail | Generated TypeScript HTTP relay transports now reject declared `Content-Length` values greater than `2_147_483_647` before payload allocator calls, success body stream reads, or no-allocator/CRM-error `arrayBuffer()` materialization. | Keep declared response length validation at the C-Two transport boundary so FastDB/WASM allocators and ordinary generated clients do not receive oversized declared payloads as allocation requests. |
| TypeScript IPC response frame length guardrail | Generated TypeScript IPC transports now reject declared frame payload lengths greater than `2_147_483_647` before calling `readExactly(payloadLen)` or reaching a payload allocator. | Keep generated direct IPC response reads bounded at the frame-header boundary; FastDB/WASM allocators should only see payload lengths after C-Two transport has rejected oversized frame declarations. |
| TypeScript IPC chunked response assembly guardrail | Generated TypeScript IPC transports now assemble chunked success replies only after validating chunk metadata, `CHUNK_LAST`, chunk indexes, stable total size/chunk count, and final `total_size` completion. | Keep generated chunked IPC response integrity in C-Two transport; FastDB payload code should receive completed payload bytes or allocator-owned payloads, not IPC chunk metadata. |
| TypeScript HTTP response length-match guardrail | Generated TypeScript HTTP relay transports now reject no-allocator success responses and 500 CRM-error binary bodies whose materialized `arrayBuffer()` byte length does not match declared `Content-Length`. | Keep binary response integrity checks consistent between allocator-backed stream reads and ordinary materialized response paths. |
| TypeScript HTTP text body length guardrail | Generated TypeScript HTTP relay transports now reject oversized declared `Content-Length` before reading non-500 data-plane error text bodies and relay resolve text bodies, including resolve error and resolve JSON responses. | Keep text-body control-plane and error-path reads behind the same transport boundary as binary payload reads without pretending JavaScript string length is a byte-level integrity check. |
| TypeScript HTTP custom header guardrail | Generated TypeScript HTTP relay transports now reject custom header names outside HTTP token syntax and values containing CR, LF, or NUL for both explicit HTTP relay and relay-aware transports; valid token names with JavaScript object prototype meaning such as `__proto__` are preserved through null-prototype normalization. | Custom header forwarding is allowed only for valid ordinary headers; generated transports must prevent header injection locally before fetch sees caller-supplied metadata and must not lose valid caller headers through object prototype semantics. |
| FastDB direct-engine BOOL producer parity | FastDB Python `ColumnEngine`, `ObjectEngine`, and fixed `Table.fill(...)` now apply the same explicit bool parser to scalar `BOOL` producer fields before C numeric writes, while mutable engines also cover native `list[BOOL]` before list writes. | Keep FastDB's own storage engines aligned with call-db, bridge, and fastdb4ts producer semantics without making C-Two core parse FastDB field metadata. |
| TypeScript data-plane transport error parity | Generated explicit HTTP relay calls now wrap fetch failures and successful/CRM-error body read failures as `C2HttpTransportError`, matching Rust's `HttpError::Transport` boundary without adding data-plane replay. | Keep transport classification useful for SDK callers while preserving the no-ambiguous-replay rule for ordinary CRM/data-plane failures. |
| TypeScript resolve payload-shape retry parity | Generated TypeScript relay-aware resolve now classifies non-array resolve payloads and malformed route payload shapes as `C2HttpTransportError`, so `maxAttempts` can retry the same class Rust maps through resolve JSON/serde transport errors. | Retry only malformed control-plane resolve payloads and other resolve transport/5xx failures; contract mismatch stays non-retryable, and data-plane calls still replay only on explicit stale-route errors. |
| FastDB-first diagnose/artifact payload guardrail | `c3 contract diagnose`, `c3 contract infer --diagnose`, and `c3 contract infer --artifacts` now validate Python diagnostic or artifact output as a JSON array of objects before writing it, so portability warning pipelines cannot silently persist invalid payloads and resource-first users can inspect inferred Python-only fallback, non-FastDB `PayloadAbiRef` warnings, plus export payload ABI artifacts before portable export or strict codegen. | Keep Python-native fallback and non-FastDB `PayloadAbiRef` values diagnostic-only for fastdb-first workflows, but make both CRM-first and resource-first CLI diagnostics and artifact bundles mechanically consumable before users rely on them. |
| FastDB codec requirement key guardrail | FastDB helper generation now preserves optional media type in codec bindings and rejects same id/hash FastDB refs whose version, schema, media type, or capabilities would produce conflicting C-Two registry keys. | Keep method wire specs, C-Two codec stubs, and FastDB codec registries keyed by the same id/version/schema/hash/media/capability identity; do not silently collapse refs by schema hash alone. |
| FastDB mutable abstract container bridge ergonomics | FastDB bridge derivation now accepts `MutableMapping` and `MutableSequence` annotations in the same mapping/sequence-shaped resource bridge families as `Mapping` and `Sequence`. | Keep bridge convenience in FastDB-owned helpers while C-Two only applies the derived `cc.bridge(...)` hooks at registration and invocation boundaries. |
| FastDB fixed tuple return bridge | FastDB bridge derivation now maps fixed CRM tuple return items through existing scalar, `Array`, single-feature, and `Batch` output converters, with arity guardrails at derivation and runtime. | Keep tuple-return adaptation as FastDB-owned bridge logic rather than a C-Two fastdb special case; C-Two should only store and invoke the resulting `cc.bridge(...)` output hook. |
| FastDB table-like batch output bridge | FastDB bridge derivation now accepts DataFrame/Arrow/Polars-style table objects for `Batch[Feature]` outputs by using row-record export methods before generic iteration, including row exporters that are not themselves iterable. | Keep dependency-specific table detection out of C-Two; FastDB should normalize row records, preserve existing row-iterable behavior for custom resource return objects, and exercise the same hook across thread-local, direct IPC, and relay. |
| FastDB table-object batch input bridge | FastDB bridge derivation now constructs table/dataframe-style resource parameters from `Batch[Feature]` row records through usable `from_records`, `from_pylist`, `from_dicts`, or one-argument constructors, with malformed batch and bad-constructor guardrails. | Keep table-library adaptation inside FastDB-owned bridge helpers; C-Two should only receive a `cc.bridge(input=...)` hook and apply it consistently across thread-local, direct IPC, and relay. |
| FastDB table-like batch input producer support | FastDB call-db serialization and derived bridge input hooks now normalize row-record exporters such as `to_pylist()`, `to_dicts()`, and `to_dict("records")` for `Batch[Feature]` inputs before batch row validation. | Keep producer-side table convenience in FastDB; C-Two should continue to see opaque call-db bytes on remote paths and derived bridge hooks on thread-local paths. |
| FastDB Batch column item annotation guardrail | FastDB bridge derivation now rejects nested or non-scalar item annotations in `Batch[Feature]` tuple-column, dict/mapping-column, and split-column resource shapes, such as `tuple[list[list[int]], list[int]]` or `Mapping[str, Sequence[list[int]]]`. | Keep grid-style column resource adaptation FastDB-owned and derivation-time checked; C-Two should only execute column bridge hooks whose column item shape is already proven scalar-compatible. |
| FastDB scalar Array bridge guardrail | FastDB `coerce_array(...)` and derived `Array[Scalar]` output hooks now reject mappings before Python can reinterpret mapping keys as array elements. | Keep scalar-array adaptation aligned with batch and column malformed-source guardrails; C-Two should keep invoking only the FastDB-owned bridge hook. |
| FastDB scalar Array producer iterable support | FastDB call-db serialization now accepts ordinary non-mapping, non-bytes-like iterables for `Array[Scalar]` inputs and outputs in both columnar and object-graph profiles, FastDB bridge derivation maps CRM `Array[Scalar]` inputs to scalar sequence resource parameters (`list[...]`, `Sequence[...]`, `Iterable[...]`, `Iterator[...]`, `MutableSequence[...]`, and aliases) or tuple resource parameters (`tuple[Scalar, ...]`, bare `tuple`, and bare `typing.Tuple`) with derivation-time annotation guardrails, and C-Two FastDB integration smokes pass tuple and sequence array inputs through thread-local/direct IPC/relay plus tuple array outputs through direct IPC/relay. | Keep scalar-array producer and bridge convenience in FastDB; C-Two should still see opaque call-db bytes remotely and only invoke derived bridge hooks locally. |
| FastDB scalar Array input annotation guardrail | FastDB bridge derivation rejects mapping, set, fixed tuple, nested sequence item, and non-scalar tuple item resource annotations for CRM `Array[Scalar]` inputs before a bridge can be registered. | Prevent list-shaped half-bridges from reaching C-Two; accepted bridge hooks must be scalar sequence or tuple conversions with explicit item coercion. |
| FastDB scalar bridge resource annotation guardrail | FastDB bridge derivation now rejects non-scalar resource annotations such as mappings or sequences for CRM scalar input/output bridges before generating a hook. | Keep scalar bridge adaptation FastDB-owned and derivation-time checked; C-Two should only execute hooks whose resource side is actually scalar-shaped. |
| FastDB single Feature bare-tuple bridge | FastDB bridge derivation now treats bare `tuple` and bare `typing.Tuple` resource annotations as schema-order field tuples for single `Feature` inputs and outputs, with runtime arity checks on outputs. | Keep bare tuple convenience in FastDB-owned bridge helpers while fixed tuple validation remains explicit and C-Two only executes the resulting bridge hooks. |
| FastDB mapping-row annotation guardrail | FastDB bridge derivation now rejects explicit mapping-shaped single-feature and batch row annotations whose key type is not `str`. | Keep schema field-name ownership explicit in FastDB bridge helpers before C-Two can register a bridge that would interpret non-string keys as feature field names. |
| FastDB variadic tuple row Batch bridge | FastDB bridge derivation now treats `tuple[Row, ...]` as an immutable `Batch[Feature]` row sequence for input and output, while fixed tuple annotations remain tuple columns. | Keep this as FastDB-owned bridge ergonomics; C-Two should only execute the resulting `cc.bridge(...)` hooks across thread-local, direct IPC, and relay. |
| FastDB batch sequence row annotation guardrail | FastDB bridge derivation now rejects explicit scalar sequence item annotations such as `list[int]` for `Batch[Feature]` input/output row sequences, while keeping bare `list` and `list[Feature]` feature-row paths intact. | Keep malformed resource annotations from becoming late runtime row-field errors; C-Two should still only apply the FastDB-owned `cc.bridge(...)` hook. |
| FastDB input hold ownership guardrail | FastDB call-db input transferables opt out of automatic C-Two input hold when `from_buffer` materializes values, and the grid direct IPC smoke proves `get_grid_infos` leaves no active `resource_input` lease. | Keep input materialization and output held views distinct; only FastDB hooks that return real resource-facing retained views should auto-select C-Two input hold. |
| FastDB raw feature input hold ownership guardrail | Raw fastdb feature transferables now use the same auto-input-hold opt-out because their `from_buffer` hook materializes feature objects rather than returning retained resource-facing views. | Keep the raw feature FastDB public surface consistent with the call-db input ownership rule even though portable CRM methods should prefer call-db envelopes. |
| FastDB free-threading row-read safety | FastDB `Table` row access, `iter_reuse()`, and fallback string lookup now serialize native `tryGetFeature(...)` row materialization with a per-table `RLock`, and the all-in-one FastDB Python suite passes under the current free-threaded Python environment. | Keep FastDB's native row materialization thread-safety inside FastDB; C-Two should only depend on stable FastDB behavior, not own FastDB's internal locks. |
| Contract version compatibility | Exact CRM contract validation exists, but semver or range-based compatibility negotiation does not. | Keep exact route validation as the safety floor. Add compatibility matching above it in Rust `c2-contract`, then expose a thin Python facade such as a version requirement on `cc.connect(...)`. |
| `auth_hook` and call metadata | C-Two does not yet provide the metadata pass-through and hook surface needed by upper layers to implement AuthN/AuthZ. | Put mechanism in C-Two, keep policy in downstream systems such as Toodle. Metadata must remain explicit and contract-scoped. |
| Dry-run hooks | The endgame architecture names dry-run hooks as an M1 closing item, but the public contract is not specified yet. | Specify the hook semantics before implementation: what is simulated, what metadata is visible, and what side effects are forbidden. |
| Async interfaces | There is no public `cc.connect_async()` or async CRM proxy/context-manager API. | Add `AsyncCRMProxy` and native-backed async call paths. Reuse Rust async IPC/HTTP internals instead of introducing a Python-only transport stack. |
| Adaptive memory lifecycle policy | Buddy pool decay, dedicated segment crash timeout, and chunk assembler timeouts are fixed-policy mechanisms. | Add Rust-owned telemetry and policy knobs first, then make timeout/decay behavior respond to observed latency and memory pressure. |
| Streaming RPC and pipeline semantics | Chunked payload transfer exists, but user-facing server/client/bidirectional streaming RPC does not. | Define stream identity, lifecycle frames, cancellation, ordering, and backpressure after the async API shape is clear. Reuse payload chunking where appropriate without conflating it with streaming calls. |

## Future

| Capability | Direction |
| --- | --- |
| Rust SDK first real slice | Start with a real Rust SDK slice that reuses `c2-wire`, `c2-ipc`, `c2-http`, and `c2-contract` instead of creating placeholder directories. |
| TypeScript client with fastdb codec | This is an endgame prerequisite for browser-native CRM clients, but it depends on the fastdb-first CRM ABI, bridge semantics, codegen helpers, and a stable compatibility story. fastdb owns the portable payload ABI and codec runtime, while C-Two owns route/contract semantics. |
| Global discovery and namespace governance | Relay mesh already handles route propagation. A broader discovery/admin surface should return candidate route metadata and must not replace production contract-scoped resolution with name-only lookup. |

## Historical References

| Document | How to use it now |
| --- | --- |
| [`docs/vision/cross-language-contract-codec-architecture.md`](./vision/cross-language-contract-codec-architecture.md) | Current architecture direction for resource-first CRM projection, `PayloadAbiRef` identity, portable export, and the fastdb-first revision. |
| [`docs/plans/2026-05-18-fastdb-first-crm-abi-and-bridge.md`](./plans/2026-05-18-fastdb-first-crm-abi-and-bridge.md) | Active implementation plan for making fastdb call-db the portable CRM payload ABI and adding the resource bridge. |
| [`docs/plans/c-two-rpc-v2-roadmap.md`](./plans/c-two-rpc-v2-roadmap.md) | Historical roadmap archive. It contains useful context, but old terminology and old paths are expected. |
| [`docs/vision/endgame-architecture.md`](./vision/endgame-architecture.md) | Long-term architecture boundary and milestone direction. Use it to understand why a capability matters, not as an implementation checklist. |
| [`docs/vision/sota-patterns.md`](./vision/sota-patterns.md) | Design-pattern reference for the RPC model and remaining high-level gaps. |
