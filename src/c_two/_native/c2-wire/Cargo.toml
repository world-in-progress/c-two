[package]
name = "c2-wire"
edition.workspace = true
version.workspace = true
description = "Wire protocol codec for C-Two IPC frames"

[features]
default = ["std"]
std = ["c2-mem"]

[dependencies]
c2-mem = { path = "../c2-mem", optional = true }
