[package]
name = "c2-alloc"
edition.workspace = true
version.workspace = true
description = "Pure buddy allocation algorithm for C-Two memory pool"

[lib]
name = "c2_alloc"

[dependencies]
libc = "0.2"
