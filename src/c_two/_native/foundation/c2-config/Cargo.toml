[package]
name = "c2-config"
edition.workspace = true
version.workspace = true
description = "Shared configuration types for C-Two IPC transport, memory pool, and relay"

[lib]
name = "c2_config"

[dependencies]
gethostname = "0.2"
uuid = { version = "1", features = ["v4"] }
