[package]
name = "c2-ipc"
edition.workspace = true
version.workspace = true
description = "Async IPC v3 client for C-Two relay"

[dependencies]
c2-wire = { path = "../c2-wire" }
tokio = { version = "1", features = ["net", "rt-multi-thread", "sync", "macros", "io-util"] }
libc = "0.2"
