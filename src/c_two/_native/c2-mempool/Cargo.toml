[package]
name = "c2-mempool"
edition.workspace = true
version.workspace = true
description = "Unified memory pool with tiered fallback for C-Two IPC"

[lib]
name = "c2_mempool"

[dependencies]
c2-alloc = { path = "../c2-alloc" }
c2-segment = { path = "../c2-segment" }
libc = "0.2"
